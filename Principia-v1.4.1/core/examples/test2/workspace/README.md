# Shared example workspace

This display-only snapshot retains the works and 15 canonical feature records used by the accepted idea. Runtime databases, embeddings, caches, and source documents are intentionally omitted.
