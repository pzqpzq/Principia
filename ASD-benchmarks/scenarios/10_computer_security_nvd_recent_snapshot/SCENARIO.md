# P100-010: NVD recent CVE JSON 2.0 snapshot

How do vulnerability attributes and publication delays relate across weakness and product groups?

Source: https://nvd.nist.gov/vuln/data-feeds

Version: rolling recent feed frozen at retrieval

Measurement coverage: Not fully established in the legacy metadata; source filenames/headers and public context preserve available dates. Release dates are not substituted for measurement dates.

Data origin: measured_or_observed

Source processing: One mutable NVD snapshot; JSON and GZIP are duplicate representations. Reporting/curation artifacts and CVSS accounting identities are not discoveries.

Interpretation limits: One mutable NVD snapshot; JSON and GZIP are duplicate representations. Reporting/curation artifacts and CVSS accounting identities are not discoveries. One mutable NVD snapshot; JSON and GZIP are duplicate representations. Reporting/curation artifacts and CVSS accounting identities are not discoveries.

Terms: U.S. Government work; see NVD data-use policy
