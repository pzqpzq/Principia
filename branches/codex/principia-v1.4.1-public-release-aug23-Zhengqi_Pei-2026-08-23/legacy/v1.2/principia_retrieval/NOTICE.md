# Archived retrieval dependency

The v1.2 workbench referenced `principia_retrieval` through a repository
symlink. This directory is a dereferenced snapshot so the legacy source tree
does not contain a broken link. It retains the framework subproject's MIT
license in `LICENSE-MIT`; the rest of the v1.2 snapshot retains its archived
Apache-2.0 license.
