# Local state omitted

The v1.2 SQLite database was generated local state rather than source code. It
is intentionally omitted from this legacy snapshot because it contained
machine-local paths. The empty artifact directories are retained so the old
application can recreate its expected layout.
