# Manifest pointer omitted

The v1.2 `latest.json` file pointed to a machine-local snapshot path. It is
intentionally omitted from this legacy archive. A fresh pointer can be produced
with the archived cloud tooling if that release is run again.
